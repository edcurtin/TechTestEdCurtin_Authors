<Window x:Class="TechTestEdCurtin.BookApplication"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:TechTestEdCurtin.Views"
        xmlns:viewModels="clr-namespace:TechTestEdCurtin.ViewModels"
        xmlns:local="clr-namespace:TechTestEdCurtin"
        mc:Ignorable="d"
        Title="BookApplication" Height="600" Width="800"
       >
    <Window.DataContext>
        <viewModels:WrapperViewModel/>
    </Window.DataContext>
    

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>

        <views:BookApplicationWrapperView 
            Grid.Row="1" 
            Grid.Column="1"/>
    </Grid>
</Window>
